import { describe, test, expect } from "vitest";
import MovieContainer from "@/components/movieCard/movieCard"; 

describe("MovieContainer", () => {
    test("should render", () => {
        expect(MovieContainer).toHaveProperty})})